run = "pip install -r requirements.txt && gunicorn server_wsgi:app -b 0.0.0.0:5000 --workers 2 --threads 2 --timeout 30"

[deployment]
run = ["sh", "-c", "pip install -r requirements.txt && gunicorn server_wsgi:app -b 0.0.0.0:5000 --workers 2 --threads 2 --timeout 30"]

[nix]
channel = "stable-24_05"
